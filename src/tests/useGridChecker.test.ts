import { describe, it, expect } from 'vitest'
import { useGridChecker } from '../composables/useGridChecker'

describe('useGridChecker', () => {
  it('should be defined', () => {
    expect(useGridChecker).toBeDefined()
  })
}) 