{
  "name": "dots2squares",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "emulator:start": "firebase emulators:start",
    "emulator:test": "node scripts/test-emulator.js",
    "firebase:deploy": "npm run build && firebase deploy",
    "firebase:deploy:hosting": "npm run build && firebase deploy --only hosting",
    "vercel:deploy": "vercel --prod",
    "vercel:dev": "vercel dev",
    "setup:github": "node scripts/setup-github-actions.js"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "mitt": "^3.0.1",
    "pinia": "^2.1.0",
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "@vitejs/plugin-vue": "^6.0.0",
    "autoprefixer": "^10.4.0",
    "cypress": "^13.0.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
